## Summary

<!-- What changed and why. Link any related issue. -->


## Checklist

- [ ] `npm run build` passes locally
- [ ] **Docs updated** if behaviour, structure, scripts or workflows changed — `README.md`, `CLAUDE.md`, and/or `docs/CCAIS-Website-Content-Guide.md` (stale docs = a bug)
- [ ] If `docs/CCAIS-Website-Content-Guide.md` changed, its PDF was regenerated (command in `CLAUDE.md`)
- [ ] Images (if any) live in `public/images/…`, are web-sized, and masters are kept in `originals/`
- [ ] Didn't delete `public/google*.html` (Search Console) or images still used by pages

<!-- Content-only PRs (e.g. adding a post) can skip the docs/PDF items. -->
